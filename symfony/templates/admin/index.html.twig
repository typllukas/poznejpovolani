{% extends 'base.html.twig' %}

{% block title %}Admin Dashboard
{% endblock %}

{% block body %}
	<div class="w-full md:w-2/3 mx-auto mb-4 flex justify-between items-center">
		<div class="text-3xl flex flex-col">
			<a href="{{ path('webinar-list') }}" class="mb-2">Webinar List</a>
			<a href="{{ path('add') }}" class="mb-2">Add New Webinar</a>
		</div>
	</div>
{% endblock %}
